# تقرير شامل عن مشروع نظام إدارة المتاجر (SaaS)

## نظرة عامة
هذا المشروع عبارة عن نظام "برمجيات كخدمة" (SaaS) لإدارة المتاجر ونقاط البيع (POS). يتيح النظام إنشاء متاجر متعددة وإدارتها من خلال لوحة تحكم مركزية (Super Admin)، بينما يمتلك كل صاحب متجر لوحة تحكم خاصة به لإدارة مخزونه ومبيعاته وموظفيه.

## التقنيات المستخدمة (Tech Stack)
- **Backend Framework**: Laravel 8.x (PHP ^7.3|^8.0)
- **Frontend**: Blade Templates, Bootstrap 5, Laravel Mix, Sass.
- **Database**: MySQL (مفترض بناءً على Laravel).
- **Libraries**:
    - `spatie/laravel-medialibrary`: لإدارة الصور والملفات.
    - `barryvdh/laravel-dompdf`: لتصدير الفواتير والتقارير بصيغة PDF.
    - `laravel/sanctum`: للمصادقة (API Authentication).
    - `guzzlehttp/guzzle`: للطلبات الخارجية (مثل خدمة الواتساب).
- **WhatsApp Integration**: يبدو أن هناك خدمة محلية (Node.js service) تعمل على المنفذ `3000` لإرسال رسائل الواتساب.

## هيكلية النظام وصلاحيات المستخدمين

### 1. السوبر أدمن (Super Admin)
يمتلك الصلاحيات الكاملة لإدارة النظام، وتشمل مهامه:
- **إدارة المتاجر**: إنشاء وتفعيل وتعطيل اشتراكات المتاجر.
- **إعدادات النظام العامة**: ضبط إعدادات المنصة.
- **إدارة خادم الواتساب**: مراقبة حالة اتصال خدمة الواتساب، إعادة تشغيل الخدمة، وتسجيل الخروج.
- **المسارات**: تبدأ بـ `/superadmin`.

### 2. صاحب المتجر (Store Owner)
يمتلك لوحة تحكم خاصة بمتجره، وتشمل:
- **نقطة البيع (POS)**:
    - واجهة بيع متكاملة.
    - إدارة الورديات (Start/End Shift) وتقاريرها.
    - البحث السريع عن المنتجات والعملاء.
    - التعامل مع مرتجعات البيع.
    - تعديل المخزون السريع من واجهة البيع.
- **إدارة المنتجات والمخزون**:
    - إضافة وتعديل المنتجات والفئات (Categories).
    - إدارة التواريخ والصلاحية (Expiry Setup): نظام متقدم لتنبيهات انتهاء الصلاحية وإتلاف المنتجات المنتهية أو تجديدها.
    - إدارة الوحدات والدفعات (Batches).
- **إدارة المشتريات والموردين**: تسجيل فواتير الشراء وإدارة جهات الاتصال (Contacts).
- **التقارير**: تقارير المبيعات، الورديات، والأرباح.
- **إعدادات المتجر**: ضبط معلومات المتجر، إعدادات الطباعة، والتنبيهات.
- **إشعارات الواتساب**: ربط رقم واتساب خاص بالمتجر لاستقبال وإرسال التنبيهات.
- **المسارات**: تبدأ بـ `/store-owner`.

## قاعدة البيانات (أهم الجداول)
بناءً على ملفات الـ Models، يحتوي النظام على الكيانات التالية:
- `Store`: بيانات المتجر (الاسم، الهاتف، الإعدادات).
- `User`: مستخدمي النظام وصلاحياتهم.
- `Product`: المنتجات وتفاصيلها الرئيسية.
- `ProductBatch`: دفعات المنتجات (تستخدم لتتبع تواريخ الصلاحية والكميات لكل دفعة).
- `Sale / SaleItem`: فواتير المبيعات وتفاصيلها.
- `Purchase / PurchaseItem`: فواتير المشتريات.
- `Shift`: نظام الورديات (تسجيل دخول وخروج الكاشير وجرد الصندوق).
- `Contact`: الموردين والعملاء.
- `Payment`: دفعات الفواتير.

## ميزات إضافية وملاحظات
- **أدوات المطور (Dev Tools)**: يحتوي النظام على مسارات (`routes`) خاصة بإصلاح المشاكل تلقائياً، مثل:
    - إصلاح روابط الصور (`/fix-images-now`).
    - إصلاح نقص أعمدة قاعدة البيانات (`/fix-database-columns`, `/fix-missing-contact-columns`).
    - مزامنة تواريخ الصلاحية.
- **إدارة الملفات**: يعتمد النظام على `Storage` و `Public` disk لعرض صور المنتجات والمرفقات.
- **التوافق**: يدعم النظام تعدد اللغات (موجود `LocaleController`).

## الخلاصة
المشروع عبارة عن نظام ERP/POS مصغر قابل للتوسع، يركز بشكل كبير على إدارة المخزون الدقيق (بالتاريخ والدفعة) وسرعة عملية البيع، مع ميزات متقدمة مثل الربط مع الواتساب وإدارة الاشتراكات.
