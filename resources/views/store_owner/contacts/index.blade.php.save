@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="m-0">إدارة جهات الاتصال (موردين / زبائن)</h5>

                    <a href="{{ route('store.contacts.create') }}" class="btn btn-primary">
                        <i class="fa fa-plus"></i> إضافة جهة اتصال جديدة
                    </a>
                </div>

                <div class="card-body">

                    @if (session('success'))
                        <div class="alert alert-success" role="alert">
                            {{ session('success') }}
                        </div>
                    @endif

                    {{-- !! -- شريط الأدوات العلوية (الفلترة، البحث، عدد السجلات) -- !! --}}
                    <div class="d-flex flex-wrap justify-content-between mb-3">

                        {{-- 1. الفلاتر (النوع) --}}
                        <div>
                            <a href="{{ route('store.contacts.index', ['type' => 'all']) }}" class="btn btn-sm btn-outline-secondary me-2">الكل</a>
                            <a href="{{ route('store.contacts.index', ['type' => 'suppliers']) }}" class="btn btn-sm btn-outline-info me-2">الموردون</a>
                            <a href="{{ route('store.contacts.index', ['type' => 'customers']) }}" class="btn btn-sm btn-outline-warning">الزبائن</a>
                        </div>

                        {{-- 2. مربع البحث --}}
                        <div class="ms-auto">
                            <input type="text" class="form-control form-control-sm" placeholder="ابحث بالاسم، الهاتف، أو الشركة..." id="search-input">
                        </div>
                    </div>

                    {{-- !! -- جدول عرض جهات الاتصال -- !! --}}
                    <table class="table table-striped table-hover" id="contacts-table">
                        <thead class="table-dark">
                            <tr>
                                <th scope="col">الاسم</th>
                                <th scope="col">الشركة</th>
                                <th scope="col">الهاتف</th>
                                <th scope="col">البريد الإلكتروني</th>
                                <th scope="col">الرقم الضريبي</th>
                                <th scope="col">العنوان</th>
                                <th scope="col">حد الدين</th>
                                <th scope="col">الإجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @php $totalCreditLimit = 0; @endphp
                            @forelse ($contacts as $contact)
                                @php $totalCreditLimit += $contact->credit_limit; @endphp
                                <tr data-contact-search="{{ $contact->contact_name }} {{ $contact->company_name }} {{ $contact->phone }}">
                                    <td>{{ $contact->contact_name }}</td>
                                    <td>{{ $contact->company_name ?? '-' }}</td>
                                    <td>{{ $contact->phone }}</td>
                                    <td>{{ $contact->email ?? '-' }}</td>
                                    <td>{{ $contact->tax_number ?? '-' }}</td>
                                    <td>{{ $contact->address ?? '-' }}</td>

                                    {{-- !! عمود حد الدين مع الألوان !! --}}
                                            @if($contact->credit_limit > 0)
                                            <i class="fa fa-arrow-up me-1"></i>
                                        @elseif($contact->credit_limit < 0)
                                            <i class="fa fa-arrow-down me-1"></i>
                                        @endif
                                        {{ number_format($contact->credit_limit, 2) }}
                                    </td>

                                    <td>
                                        <a href="{{ route('store.contacts.edit', $contact->id) }}" class="btn btn-sm btn-outline-secondary">تعديل</a>
                                        <form action="{{ route('store.contacts.destroy', $contact->id) }}" method="POST" style="display: inline-block;" onsubmit="return confirm('هل أنت متأكد أنك تريد حذف هذه الجهة؟');">
                                            @csrf @method('DELETE')
                                            <button type="submit" class="btn btn-sm btn-outline-danger">حذف</button>
                                        </form>
                                    </td>
                                </tr>
                            @empty
                                <tr>
                                    <td colspan="8" class="text-center">لا توجد جهات اتصال مسجلة حالياً.</td>
                                </tr>
                            @endforelse
                        </tbody>

                        {{-- !! الصف الخاص بمجموع حد الدين !! --}}
                        <tfoot>
                            <tr>
                                <th colspan="6" class="text-end">الإجمالي العام لحد الدين / الرصيد:</th>
                                <th style="font-weight: bold; color: {{ $totalCreditLimit >= 0 ? '#198754' : '#dc3545' }};">
                                    {{ number_format($totalCreditLimit, 2) }}
                                </th>
                                <th></th>
                            </tr>
                        </tfoot>
                    </table>

                    {{-- !! كود JavaScript للبحث في الجدول (Search Box) !! --}}
                    @section('scripts')
                    <script>
                        document.addEventListener('DOMContentLoaded', function() {
                            const searchInput = document.getElementById('search-input');
                            const tableRows = document.querySelectorAll('#contacts-table tbody tr[data-contact-search]');

                            searchInput.addEventListener('keyup', function() {
                                const searchTerm = searchInput.value.toLowerCase();
                                tableRows.forEach(row => {
                                    const searchData = row.getAttribute('data-contact-search').toLowerCase();
                                    if (searchData.includes(searchTerm)) {
                                        row.style.display = '';
                                    } else {
                                        row.style.display = 'none';
                                    }
                                });
                            });
                        });
                    </script>
                    @endsection
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
